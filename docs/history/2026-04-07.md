# 2026-04-07 开发记录

**文档作用：** 记录 C.5 state clone 覆盖扩展 checkpoint，保证“先失败后修复”书签与恢复命令可直接复现。

**Last updated:** 2026-04-07

## Checkpoint（C.5：state helper 内联克隆补齐 `MatchCases/Cases`）

### 当前状态

- 先失败后修复（TDD 书签）：
  - 新增 `StyioDiagnostics.StateInlineMatchCasesFunctionUsesCallArgument`；
  - 红灯阶段可复现 `Styio.TypeError: unsupported AST node in inlined state expression clone`（exit `4`）。
- 修复内容：
  - `ToStyioIR.cpp::clone_state_expr_with_subst` 新增 `CasesAST` 克隆分支；
  - `ToStyioIR.cpp::clone_state_expr_with_subst` 新增 `MatchCasesAST` 克隆分支；
  - 保持边界检查：`MatchCases` 克隆后若非 `CasesAST`，仍抛 `TypeError`。
- 回归结果：
  - `StyioDiagnostics.SingleArgStateFunctionInliningUsesCallArgument` 全绿；
  - `StyioDiagnostics.BlockStateFunctionInliningUsesCallArgument` 全绿；
  - `StyioDiagnostics.StateInlineMatchCasesFunctionUsesCallArgument` 全绿（输出 `10/12/15`）；
  - `ctest -L styio_pipeline` 全绿（32/32）；
  - `ctest -L security` 全绿（112/112）；
  - `build-asan-ubsan` 下新用例全绿（1/1）。
- ADR 落地：`docs/adr/ADR-0082-state-inline-matchcases-clone-coverage.md`。

### 下一步

1. C.5：继续扩展 state clone 覆盖，优先补齐高频表达式节点，减少 unsupported fallback。
2. C.6：保持每次 clone 覆盖扩展后的 sanitizer 验证（`styio_pipeline + security`）。
3. D.5：把 state inline 失败样本纳入恢复演练首批健康检查。

### 复现命令

```bash
cmake -S . -B build
cmake --build build --target styio_test -j8
ctest --test-dir build -R '^StyioDiagnostics\.(SingleArgStateFunctionInliningUsesCallArgument|BlockStateFunctionInliningUsesCallArgument|StateInlineMatchCasesFunctionUsesCallArgument)$' --output-on-failure
ctest --test-dir build -L styio_pipeline --output-on-failure
ctest --test-dir build -L security --output-on-failure

cmake --build build-asan-ubsan --target styio_test -j8
ASAN_OPTIONS='detect_leaks=0:halt_on_error=1:abort_on_error=1' \
UBSAN_OPTIONS='print_stacktrace=1:halt_on_error=1' \
ctest --test-dir build-asan-ubsan -R '^StyioDiagnostics\.StateInlineMatchCasesFunctionUsesCallArgument$' --output-on-failure
```

## Checkpoint（D.3：state helper `MatchCases` 内联 Soak 门禁）

### 当前状态

- 目标：
  - 将 `ADR-0082` 新增的 `MatchCases/Cases` 克隆路径纳入长跑守门，避免仅有单次单元覆盖。
- 先失败后修复（TDD 书签）：
  - 新增 `StyioSoakSingleThread.StateInlineMatchCasesProgramLoop`；
  - 样例固定为 `# pulse = (x) => @[sum = 0](out = x ?= {...})`；
  - 每轮断言输出稳定 `10/12/15`。
- 修复内容：
  - `tests/soak/styio_soak_test.cpp`：新增 `StateInlineMatchCasesProgramLoop`。
  - `tests/CMakeLists.txt`：新增 `soak_deep_state_inline_matchcases_program`（`STYIO_SOAK_STATE_MATCH_ITERS=1500`）。
  - `tests/soak/README.md`：补齐新用例与 deep 环境变量。
- 回归结果：
  - `ctest -R StateInlineMatchCasesProgramLoop` 全绿（1/1）；
  - `ctest -R soak_deep_state_inline_matchcases_program` 全绿（1/1）；
  - `ctest -L soak_smoke` 全绿（8/8）；
  - `ctest -L styio_pipeline` 全绿（32/32）。
- ADR 落地：`docs/adr/ADR-0083-state-inline-matchcases-soak-gate.md`。

### 下一步

1. D.3：继续补 state inline 的复合表达式长跑样本（优先历史 crash/fuzz 样本）。
2. D.4：保持新增 soak_deep gate 与 nightly 工作流同步。
3. C.6：对 state inline 相关改动保持 sanitizer 组测节奏。

### 复现命令

```bash
cmake -S . -B build
cmake --build build --target styio_soak_test -j8
ctest --test-dir build -R StateInlineMatchCasesProgramLoop --output-on-failure
ctest --test-dir build -R soak_deep_state_inline_matchcases_program --output-on-failure
ctest --test-dir build -L soak_smoke --output-on-failure
ctest --test-dir build -L styio_pipeline --output-on-failure
```

## Checkpoint（C.5：state helper 内联克隆补齐 `InfiniteAST`）

### 当前状态

- 先失败后修复（TDD 书签）：
  - 新增 `StyioDiagnostics.StateInlineInfiniteLiteralFunctionUsesCallArgument`；
  - 红灯阶段稳定复现 `Styio.TypeError: unsupported AST node in inlined state expression clone: 75`（exit `4`）。
- 修复内容：
  - `ToStyioIR.cpp::clone_state_expr_with_subst` 新增 `InfiniteAST` 克隆分支；
  - 支持 `InfiniteType::Original` 与 `InfiniteType::Incremental` 两种形态克隆。
- 回归结果：
  - `StyioDiagnostics.StateInlineInfiniteLiteralFunctionUsesCallArgument` 全绿（输出 `0/0`）；
  - 相关 state inline 组合回归全绿（`1/3/6`、`10/12/15`、`0/0`）；
  - `ctest -L styio_pipeline` 全绿（33/33）；
  - `ctest -L security` 全绿（112/112）；
  - `build-asan-ubsan` 下新用例全绿（1/1）。
- ADR 落地：`docs/adr/ADR-0084-state-inline-infinite-clone-and-soak-gate.md`。

### 下一步

1. C.5：继续扩展 state clone 覆盖，优先处理仍可能在内联路径出现的控制流节点（如 `CondFlow` 组合形态）。
2. C.6：每次 clone 扩展后保持 `security + styio_pipeline + ASan/UBSan` 验证节奏。
3. D.5：把 `unsupported AST node` 回归样本持续沉淀到恢复演练首批健康检查。

### 复现命令

```bash
cmake -S . -B build
cmake --build build --target styio_test -j8
ctest --test-dir build -R '^StyioDiagnostics\.StateInlineInfiniteLiteralFunctionUsesCallArgument$' --output-on-failure
ctest --test-dir build -L styio_pipeline --output-on-failure
ctest --test-dir build -L security --output-on-failure

cmake --build build-asan-ubsan --target styio_test -j8
ASAN_OPTIONS='detect_leaks=0:halt_on_error=1:abort_on_error=1' \
UBSAN_OPTIONS='print_stacktrace=1:halt_on_error=1' \
ctest --test-dir build-asan-ubsan -R '^StyioDiagnostics\.StateInlineInfiniteLiteralFunctionUsesCallArgument$' --output-on-failure
```

## Checkpoint（D.3：state helper `InfiniteAST` 内联 Soak 门禁）

### 当前状态

- 目标：
  - 将 `ADR-0084` 新增的 `InfiniteAST` 内联克隆路径纳入长跑守门。
- 先失败后修复（TDD 书签）：
  - 新增 `StyioSoakSingleThread.StateInlineInfiniteProgramLoop`；
  - 样例固定为 `# pulse = (x) => @[sum = 0](out = [...])`；
  - 每轮断言输出稳定 `0/0`。
- 修复内容：
  - `tests/soak/styio_soak_test.cpp`：新增 `StateInlineInfiniteProgramLoop`；
  - `tests/CMakeLists.txt`：新增 `soak_deep_state_inline_infinite_program`（`STYIO_SOAK_STATE_INFINITE_ITERS=1500`）；
  - `tests/soak/README.md`：补齐新用例与 deep 环境变量。
- 回归结果：
  - `ctest -R StateInlineInfiniteProgramLoop` 全绿（1/1）；
  - `ctest -R soak_deep_state_inline_infinite_program` 全绿（1/1）；
  - `ctest -L soak_smoke` 全绿（9/9）；
  - `ctest -L styio_pipeline` 全绿（33/33）。

### 下一步

1. D.3：继续补 state inline 的“历史崩溃样本优先”长跑用例池。
2. D.4：保持新增 deep gate 与 nightly 同步（防止只在本地覆盖）。
3. C.6：对所有新增 state inline 路径持续跑 sanitizer 目标回归。

### 复现命令

```bash
cmake -S . -B build
cmake --build build --target styio_soak_test -j8
ctest --test-dir build -R '^StyioSoakSingleThread\.StateInlineInfiniteProgramLoop$' --output-on-failure
ctest --test-dir build -R '^soak_deep_state_inline_infinite_program$' --output-on-failure
ctest --test-dir build -L soak_smoke --output-on-failure
ctest --test-dir build -L styio_pipeline --output-on-failure
```

## Checkpoint（E.6：Parser 默认引擎切换到 `new`）

### 当前状态

- 变更内容：
  - `main.cpp` 将 `--parser-engine` 默认值从 `legacy` 切到 `new`；
  - 解析入口初始化默认引擎同步为 `StyioParserEngine::New`；
  - 保留 `--parser-engine=legacy` 显式回退能力。
- 先失败后修复（TDD 书签）：
  - 新增 `StyioParserEngine.DefaultEngineIsNewInShadowArtifact`；
  - 通过 `--parser-shadow-compare --parser-shadow-artifact-dir` 读取 JSONL 工件，断言默认 `primary_engine` 为 `new`。
- 回归结果：
  - 新用例全绿（1/1）；
  - `ctest -L styio_pipeline` 全绿（34/34）；
  - `ctest -L security` 全绿（112/112）；
  - `ctest -L milestone` 全绿（92/92）。
- ADR 落地：`docs/adr/ADR-0085-parser-default-switch-to-new-with-legacy-fallback.md`。

### 下一步

1. E.6/E.7：继续观测 shadow gate 连续窗口，确认默认切换后无差异漂移。
2. E.7：在稳定窗口内保留 `legacy` 回退并逐步压缩剩余 fallback 面。
3. F.3：继续 parser 边界巡检，防止默认切换后暴露新的错误路径异常。

### 复现命令

```bash
cmake -S . -B build
cmake --build build --target styio_test -j8
ctest --test-dir build -R '^StyioParserEngine\.DefaultEngineIsNewInShadowArtifact$' --output-on-failure
ctest --test-dir build -L styio_pipeline --output-on-failure
ctest --test-dir build -L security --output-on-failure
ctest --test-dir build -L milestone --output-on-failure
```

## Checkpoint（D.5：一键恢复脚本 `checkpoint-health`）

### 当前状态

- 目标：
  - 将 parser 默认切换 + state inline + soak + pipeline/security 的恢复链路收敛为单命令。
- 交付内容：
  - 新增 `scripts/checkpoint-health.sh`；
  - 支持 `--build-dir`、`--asan-build-dir`、`--no-asan` 参数；
  - 固化阶段输出前缀 `[checkpoint-health]` 便于日志定位。
- 覆盖范围：
  - `StyioParserEngine.DefaultEngineIsNewInShadowArtifact`；
  - state inline 诊断回归（`1/3/6`、`10/12/15`、`0/0`）；
  - state inline soak smoke + deep 三条；
  - `styio_pipeline`、`security` 标签；
  - 可选 ASan/UBSan 目标回归。
- 验证结果：
  - `./scripts/checkpoint-health.sh --no-asan` 全绿（含 deep soak + pipeline + security）。
- ADR 落地：`docs/adr/ADR-0086-checkpoint-health-batch-script.md`。

### 下一步

1. D.5：将后续新增高风险回归优先并入 `checkpoint-health`，避免恢复脚本与现实路径漂移。
2. D.4：nightly 任务按需复用该脚本的测试清单，保持本地/CI 一致。
3. F.3：若新增 parser 边界回归，优先纳入脚本的快速检查段。

### 复现命令

```bash
./scripts/checkpoint-health.sh --no-asan
# 或完整含 ASan/UBSan
./scripts/checkpoint-health.sh
```

## Checkpoint（E.7：NewParser Block/Control 子集接管 Hash Block Body）

### 当前状态

- 先失败后修复（TDD 书签）：
  - 新增 `StyioSecurityNewParserStmt.SubsetStartGateIncludesBlockAndControlStarters`；
  - 新增 `StyioSecurityNewParserStmt.MatchesLegacyOnBlockControlSubsetSamples`；
  - 红灯阶段 `parse_main_block_new_subset(...)` 在 block/control 样例上会抛 `StyioSyntaxError("unexpected statement token in new parser subset")`。
- 修复内容：
  - `NewParserExpr.cpp` 扩展 statement subset token/start gate，纳入 `{}` block、`<<`、`^`、`>>`、`...`、`|>`；
  - 新增 `parse_block_only_new_subset(...)` 与 `parse_block_with_forward_new_subset(...)`；
  - `parse_hash_stmt_new_subset(...)` 的 `# ... => { ... }` body 改走 new block subset，而不是 legacy `parse_block_with_forward(...)`。
- 回归结果：
  - `ctest -R '^StyioSecurityNewParserStmt\\.'` 全绿（12/12）；
  - `ctest -L styio_pipeline` 全绿（34/34）；
  - `ctest -L security` 全绿（114/114）。
- ADR 落地：`docs/adr/ADR-0087-new-parser-block-control-subset.md`。

### 下一步

1. E.7：继续压缩 block 内剩余 legacy 边界，优先评估 `match/cases` 与 `@` 相关语句的子集接管切片。
2. E.7：评估 `parse_main_block_new_shadow(...)` 是否可以从“整文件 token 白名单”向“更细粒度 fallback”推进。
3. F.3：继续沿 parser 错误路径巡检，优先锁定默认 `new` 主路径下的内部异常泄露点。

### 风险

1. 当前仅接管 block/control 主体；block 尾随 forward 列表仍复用 legacy `parse_forward_as_list(...)`。
2. 更复杂的 loop / `@` / `match-cases` block 体若超出新子集，当前仍可能通过 hash 外层保守回退到 legacy。

### 复现命令

```bash
cmake -S . -B build
cmake --build build --target styio_security_test -j8
ctest --test-dir build -R '^StyioSecurityNewParserStmt\.' --output-on-failure
ctest --test-dir build -L styio_pipeline --output-on-failure
ctest --test-dir build -L security --output-on-failure
```

## Checkpoint（E.7：NewParser 接管表达式 Postfix `?=` Match-Cases）

### 当前状态

- 先失败后修复（TDD 书签）：
  - 新增 `StyioSecurityNewParserStmt.MatchesLegacyOnMatchCasesSubsetSamples`；
  - 新增 `StyioParserEngine.LegacyAndNewMatchOnM3MatchExprSample`；
  - 红灯阶段 `parse_main_block_new_subset(...)` 在 `label = x % 2 ?= { ... }` 这类输入上无法继续前进，只能依赖 legacy fallback。
- 修复内容：
  - `NewParserExpr.cpp` 为完整表达式新增 postfix 处理，仅接管 `?={...}`；
  - `parse_expr_new_subset(...)`、括号子表达式与函数调用参数现均可识别 `MatchCasesAST`；
  - cases body 暂时继续复用 legacy `parse_cases_only(context)`，保持切片范围受控。
- 回归结果：
  - `ctest -R '^StyioSecurityNewParserStmt\.'` 全绿（13/13）；
  - `ctest -R '^StyioParserEngine\.LegacyAndNewMatchOnM3MatchExprSample$'` 全绿（1/1）；
  - `ctest -L styio_pipeline` 全绿（35/35）；
  - `ctest -L security` 全绿（115/115）。
- ADR 落地：`docs/adr/ADR-0088-new-parser-matchcases-postfix-subset.md`。

### 下一步

1. E.7：继续压缩 postfix 相关剩余 legacy 边界，优先评估 iterator / redirect 是否能拆成独立子切片。
2. E.7：继续评估 `parse_main_block_new_shadow(...)` 的整文件 token gate 是否可以细化为更小粒度回退。
3. F.3：继续巡检默认 `new` 主路径在 parser 错误场景下的异常边界，避免内部异常文案泄露。

### 风险

1. 当前只接管 `?={...}`；其它 postfix 仍未纳入 new subset。
2. `cases` body 继续调用 legacy `parse_cases_only(...)`，尚未完全摆脱旧路径。

### 复现命令

```bash
cmake -S . -B build
cmake --build build --target styio_security_test styio_test -j8
ctest --test-dir build -R '^StyioSecurityNewParserStmt\.' --output-on-failure
ctest --test-dir build -R '^StyioParserEngine\.LegacyAndNewMatchOnM3MatchExprSample$' --output-on-failure
ctest --test-dir build -L styio_pipeline --output-on-failure
ctest --test-dir build -L security --output-on-failure
```

## Checkpoint（E.7：NewParser 主入口改为逐语句 Fallback，并输出 Route Stats）

### 当前状态

- 先失败后修复（TDD 书签）：
  - 新增 `StyioParserEngine.ShadowArtifactDetailCapturesPerStatementFallbackStats`；
  - 用混合程序（subset 语句 + unsupported 语句）读取 shadow artifact，确认 `detail` 字段包含 `primary_route=...legacy_fallback_statements=...`；
  - 红灯阶段无法观测 `new` 主路径内部路由，且任一 unsupported token 会让整文件直接回退 legacy。
- 修复内容：
  - `Parser.cpp::parse_main_block_new_shadow(...)` 从“整文件 token gate”改为“逐语句先尝试 new subset，失败再回退当前语句 legacy”；
  - 新增 `StyioParserRouteStats`，记录 `new_subset_statements` 与 `legacy_fallback_statements`；
  - `main.cpp` 将 route stats 写入 shadow artifact `detail`；
  - `NewParserExpr.cpp` 为 unsupported continuation（如 `<-` / `>>` / `>` / `[` / `|` / `<<`）显式抛异常，避免 name-led 语句被 new expr 子集吃半截后再在下一 token 处炸成 ParseError。
- 回归结果：
  - `ctest -R '^StyioParserEngine\.(PointerScrutineeMatchDoesNotAbortAndReportsTypeError|ShadowArtifactDetailCapturesPerStatementFallbackStats)$'` 全绿（2/2）；
  - `ctest -L styio_pipeline` 全绿（36/36）；
  - `ctest -L security` 全绿（115/115）。
- ADR 落地：`docs/adr/ADR-0089-new-parser-per-statement-fallback-and-route-stats.md`。

### 下一步

1. E.7：继续减少 route stats 中的 `legacy_fallback_statements`，优先拆 iterator / redirect / index-op 这类仍会触发 continuation fallback 的表达式尾缀。
2. E.7：继续把 route stats 接入更多恢复脚本或 nightly 统计，避免只在单测里可见。
3. F.3：沿新主路径继续巡检“吃半截 expression”类错误，新增样本即转成显式 fallback 或稳定诊断。

### 风险

1. 当前 route stats 只统计主入口语句级路由，不代表 block/cases/iterator 内部已完全去 legacy。
2. `unsupported continuation` 列表仍是白名单之外的保守拒绝，后续每接管一种 postfix 都需要同步收缩该列表。

### 复现命令

```bash
cmake -S . -B build
cmake --build build --target styio_test styio_security_test -j8
ctest --test-dir build -R '^StyioParserEngine\.(PointerScrutineeMatchDoesNotAbortAndReportsTypeError|ShadowArtifactDetailCapturesPerStatementFallbackStats)$' --output-on-failure
ctest --test-dir build -L styio_pipeline --output-on-failure
ctest --test-dir build -L security --output-on-failure
```

## Checkpoint（E.7：NewParser 接管资源写入/重定向 Postfix 子集）

### 当前状态

- 先失败后修复（TDD 书签）：
  - 新增 `StyioSecurityNewParserStmt.MatchesLegacyOnResourcePostfixSubsetSamples`；
  - 新增 `StyioParserEngine.LegacyAndNewMatchOnM5WriteFileSample`；
  - 新增 `StyioParserEngine.LegacyAndNewMatchOnM5RedirectSample`；
  - 新增 `StyioParserEngine.ShadowArtifactDetailShowsZeroFallbackForResourcePostfixSubset`；
  - 红灯阶段 `expr >> @file{...}` / `expr -> @file{...}` 在 `new` 主路径上会命中 `unsupported continuation`，只能逐语句回退到 legacy。
- 修复内容：
  - `NewParserExpr.cpp` 在 postfix 阶段新增资源后缀接管；
  - `expr >> @file{...}` / `expr >> @{...}` 现在直接构造 `ResourceWriteAST`；
  - `expr -> @file{...}` / `expr -> @{...}` 现在直接构造 `ResourceRedirectAST`；
  - `>>` 若后面不是 `@...`，仍维持稳定异常并由外层 fallback 接管，不把 iterator tail 混进本切片。
- 回归结果：
  - `ctest --test-dir build -R 'ResourcePostfix|M5WriteFile|M5Redirect|ZeroFallbackForResourcePostfix' --output-on-failure` 全绿（4/4）；
  - `ctest --test-dir build -L styio_pipeline` 全绿（39/39）；
  - `ctest --test-dir build -L security` 全绿（117/117）。
- ADR 落地：`docs/adr/ADR-0090-new-parser-resource-postfix-subset.md`。

### 下一步

1. E.7：继续压缩表达式尾缀 fallback，优先评估 `x[i]` / token-index 这一类仍会触发 `unsupported continuation` 的路径。
2. E.7：继续拆分 `>>` 的非资源语义，把 iterator tail 与资源写入保持分离推进。
3. F.3：继续用 route stats 巡检默认 `new` 主路径，优先处理“看起来像 subset，实际还会半途回退”的尾缀样例。

### 风险

1. 当前只接管资源型 postfix；`>>` 的 iterator tail 仍走 legacy fallback。
2. 以 `@file{...}` 开头的流式输入语句不在这个 checkpoint 范围内，仍需后续独立切片。

### 复现命令

```bash
cmake -S . -B build
cmake --build build --target styio_test styio_security_test -j8
ctest --test-dir build -R 'ResourcePostfix|M5WriteFile|M5Redirect|ZeroFallbackForResourcePostfix' --output-on-failure
ctest --test-dir build -L styio_pipeline --output-on-failure
ctest --test-dir build -L security --output-on-failure
```

## Checkpoint（E.7：NewParser 接管 Name-Iterator 与 Instant Pull 子集）

### 当前状态

- 先失败后修复（TDD 书签）：
  - 新增 `StyioSecurityNewParserStmt.MatchesLegacyOnIteratorStmtSubsetSamples`；
  - 新增 `StyioSecurityNewParserStmt.MatchesLegacyOnInstantPullSubsetSamples`；
  - 新增 `StyioParserEngine.LegacyAndNewMatchOnM5ReadFileSample` / `LegacyAndNewMatchOnM5AutoDetectSample` / `LegacyAndNewMatchOnM5PipeFuncSample` / `LegacyAndNewMatchOnM7InstantPullSample`；
  - 新增 `StyioParserEngine.ShadowArtifactDetailShowsZeroFallbackForIteratorSubset`；
  - 红灯阶段 `f >> #(line) => { ... }` 与 `(<< @file{...})` 都会触发入口级 fallback。
- 修复内容：
  - `NewParserExpr.cpp` 的 name-led stmt 分支新增 `name >> ...`，直接复用 `parse_iterator_only(...)`；
  - `(` primary 分支新增 instant pull 识别，直接构造 `InstantPullAST`；
  - 本切片明确不接管 collection-start iterator（例如 `@file{...} >> ...` / `[1,2,3] >> ...`）。
- 回归结果：
  - `ctest --test-dir build -R 'IteratorSubset|InstantPullSubset|M5ReadFile|M5AutoDetect|M5PipeFunc|M7InstantPull' --output-on-failure` 全绿（6/6）；
  - `ctest --test-dir build -L styio_pipeline` 全绿（44/44）；
  - `ctest --test-dir build -L security` 全绿（119/119）。
- 额外验证：
  - `scripts/parser-shadow-suite-gate.sh ./build/bin/styio ./tests/milestones/m5 ...` 仍因 `t06_fail_fast` 运行期失败返回非零，但所有 shadow artifact 仍为 `match`；
  - M5 全套 route stats 已降到 `legacy_fallback_statements=0`；
  - M7 `t04_instant_pull` 已降到 `new_subset_statements=3,legacy_fallback_statements=0`。
- ADR 落地：`docs/adr/ADR-0091-new-parser-iterator-and-instant-pull-subset.md`。

### 下一步

1. E.7：继续收 `@[...]` 起始语句，优先把 snapshot/state decl 从入口级 fallback 中摘出来。
2. E.7：将主要剩余 fallback 缩到 collection-start iterator（`@file... >>` / `[1,2,3] >>`）。
3. F.3：继续用 route stats 观测“入口级零回退但内部仍复用 legacy helper”的边界，避免误判完成度。

### 风险

1. `name >> ...` 当前只是在入口级复用稳定叶子 helper，不代表完整 iterator grammar 已由 new parser 原生拥有。
2. collection-start iterator 仍未收口，M7 的 zip/stream 类样例仍大量依赖 legacy。

### 复现命令

```bash
cmake -S . -B build
cmake --build build --target styio_test styio_security_test -j8
ctest --test-dir build -R 'IteratorSubset|InstantPullSubset|M5ReadFile|M5AutoDetect|M5PipeFunc|M7InstantPull' --output-on-failure
ctest --test-dir build -L styio_pipeline --output-on-failure
ctest --test-dir build -L security --output-on-failure
tmp_dir=$(mktemp -d)
./scripts/parser-shadow-suite-gate.sh ./build/bin/styio ./tests/milestones/m5 "$tmp_dir"
```

## Checkpoint（E.7：NewParser 接管 `@[...]` Snapshot / State Decl 起始子集）

### 当前状态

- 先失败后修复（TDD 书签）：
  - 新增 `StyioSecurityNewParserStmt.MatchesLegacyOnSnapshotDeclSubsetSamples`；
  - 新增 `StyioParserEngine.LegacyAndNewMatchOnM7SnapshotSample`；
  - 新增 `StyioParserEngine.ShadowArtifactDetailShowsZeroFallbackForSnapshotDeclSubset`；
  - 红灯阶段 `@[...]` 起始语句不在 subset start gate 内，只能直接回退 legacy。
- 修复内容：
  - `parse_state_decl_after_at(...)` 提升为可复用接口；
  - `new` stmt subset 新增 `TOK_AT` 起始支持，但只接管 `@[...]`；
  - 非 `@[...]` 的 `@...` 起始语句继续稳定 fallback，不混入本切片；
  - subset token/start gate 同步纳入 `TOK_AT` / `[` / `]`。
- 回归结果：
  - `ctest --test-dir build -R 'Snapshot.*Subset|M7SnapshotSample' --output-on-failure` 全绿（3/3）；
  - `ctest --test-dir build -L styio_pipeline` 全绿（46/46）；
  - `ctest --test-dir build -L security` 全绿（120/120）。
- 额外验证：
  - `scripts/parser-shadow-suite-gate.sh ./build/bin/styio ./tests/milestones/m7 ...` 全绿（20/20 artifacts match）；
  - `t03_snapshot` 从 `legacy_fallback_statements=2` 降到 `1`；
  - `t06_snapshot_lock` 从 `3` 降到 `2`；
  - `t09_snapshot_accum` 从 `3` 降到 `2`。
- ADR 落地：`docs/adr/ADR-0092-new-parser-at-snapshot-subset.md`。

### 下一步

1. E.7：剩余主要入口级 fallback 已集中在 collection-start iterator：
   - `@file{...} >> ...`
   - `[1,2,3] >> ...`
2. E.7：下一刀优先考虑只接管 collection atom 起始，不把 zip/forward/cases 连锁语义一次性拉入。
3. F.3：继续用 route stats 监控 M7 套件，避免新增样例时回退比例反弹。

### 风险

1. 当前 `TOK_AT` 只对 `@[...]` 开口；`@file{...}` / `@(…)` 仍会保守回退。
2. M7 剩余样例的主要难点不再是 `@[...]`，而是 iterator / zip 主体 grammar。

### 复现命令

```bash
cmake -S . -B build
cmake --build build --target styio_test styio_security_test -j8
ctest --test-dir build -R 'Snapshot.*Subset|M7SnapshotSample' --output-on-failure
ctest --test-dir build -L styio_pipeline --output-on-failure
ctest --test-dir build -L security --output-on-failure
tmp_dir=$(mktemp -d)
./scripts/parser-shadow-suite-gate.sh ./build/bin/styio ./tests/milestones/m7 "$tmp_dir"
```

## Checkpoint（E.7：NewParser 接管 `@file... >> ...` 资源流入口）

### 当前状态

- 先失败后修复（TDD 书签）：
  - 新增 `StyioSecurityNewParserStmt.MatchesLegacyOnAtResourceSubsetSamples`；
  - 新增 `StyioParserEngine.LegacyAndNewMatchOnM7ZipFilesSample` / `LegacyAndNewMatchOnM7ArbitrageSample` / `LegacyAndNewMatchOnM7FullPipelineSample`；
  - 新增 `StyioParserEngine.ShadowArtifactDetailShowsZeroFallbackForAtResourceSubset`；
  - 红灯阶段 `@file{...} >> ...` 仍被 `new` 入口拒绝，只能逐语句 fallback 到 legacy。
- 修复内容：
  - 把 legacy 的 `TOK_AT` 入口分发提炼为 `parse_at_stmt_or_expr(...)`；
  - `parse_stmt_or_expr(...)` 与 `new` stmt subset 统一复用该 helper；
  - `@[...]`、`@(…)`、`@file{...}` / `@{...}` 起始语句现在都可以由 `new` 入口接住；
  - 本切片明确不处理 `[ ... ] >> ...` list-start iterator。
- 回归结果：
  - `ctest --test-dir build -R 'AtResourceSubset|M7ZipFilesSample|M7ArbitrageSample|M7FullPipelineSample' --output-on-failure` 全绿（5/5）；
  - `ctest --test-dir build -L styio_pipeline` 全绿（50/50）；
  - `ctest --test-dir build -L security` 全绿（121/121）。
- 额外验证：
  - `scripts/parser-shadow-suite-gate.sh ./build/bin/styio ./tests/milestones/m7 ...` 全绿（20/20 artifacts match）；
  - `t05_zip_files` 已降到 `new_subset_statements=1,legacy_fallback_statements=0`；
  - `t08_arbitrage` 已降到 `1,0`；
  - `t10_full_pipeline` 已降到 `1,0`；
  - 剩余非零 fallback 仅见 `t01_zip_collections`、`t02_zip_unequal` 与含 `[1,2,3] >> ...` 的 snapshot 组合样例。
- ADR 落地：`docs/adr/ADR-0093-new-parser-at-resource-iterator-entry.md`。

### 下一步

1. E.7：剩余入口级 fallback 现在主要只剩 `[ ... ] >> ...` list-start iterator。
2. E.7：下一刀应避免一次性实现完整 zip grammar，先只把 list/tuple collection atom 交给可复用 helper。
3. F.3：继续以 M7 shadow gate 为主观测窗口，确认 route stats 不再因新增资源流样例反弹。

### 风险

1. `@...` 入口虽然已接住，但内部 iterator/zip 语义仍复用 legacy helper，不代表已完成彻底切换。
2. `[ ... ]` 起始语句仍未收口，M7 的最后一批 fallback 还在这条线上。

### 复现命令

```bash
cmake -S . -B build
cmake --build build --target styio_test styio_security_test -j8
ctest --test-dir build -R 'AtResourceSubset|M7ZipFilesSample|M7ArbitrageSample|M7FullPipelineSample' --output-on-failure
ctest --test-dir build -L styio_pipeline --output-on-failure
ctest --test-dir build -L security --output-on-failure
tmp_dir=$(mktemp -d)
./scripts/parser-shadow-suite-gate.sh ./build/bin/styio ./tests/milestones/m7 "$tmp_dir"
```
