# 开发记录 — 2026-04-03

**文档作用：** 记录本日 Checkpoint 执行落地（构建基线、错误模型、会话所有权空壳、CI/ADR），作为中断恢复锚点。

**Last updated:** 2026-04-03

## 摘要

- 落地 A.1/A.2：移除仓库内已跟踪平台二进制（`styio*`），构建产物统一输出到 `build/bin`，测试目标路径改为 `$<TARGET_FILE:styio>`。
- 落地 A.3（最小 CI）：新增 `.github/workflows/ci.yml`，包含 configure/build/security/pipeline/milestone smoke。
- 落地 B.1/B.2/B.3：
  - 新增 `StyioLexError` / `StyioTypeError`。
  - `Tokenizer` 修复未闭合字符串、未闭合块注释、行注释 EOF 边界。
  - `main` 引入分阶段错误分类与稳定退出码，新增 `--error-format=text|jsonl`。
- 落地 B.4/B.5（首版）：
  - 新增 `tests/fuzz/`（`styio_fuzz_lexer`、`styio_fuzz_parser`）与 seed corpus。
  - CI 加入 fuzz smoke（`fuzz_smoke` 标签）。
  - 新增夜间深跑工作流 `.github/workflows/nightly-fuzz.yml` 并归档 fuzz artifacts。
- 落地 C.1/C.2（首段）：
  - 新增 `CompilationSession`，由会话集中接管 Token/Context/AST/IR 生命周期。
  - 新增 `StyioHandleTable` 空壳类型。
  - `StyioContext` 补析构释放 `ast_repr`。
- Unicode 边界收敛：
  - 新增 `StyioUnicode` 防腐层（`src/StyioUnicode/Unicode.hpp/.cpp`）。
  - Parser/Tokenizer 不再直接调用 `std::is*`，统一走 `StyioUnicode::*`。
  - `STYIO_USE_ICU=ON` 时仅在防腐层内部启用 ICU codepoint 分类能力。
- 里程碑回归修复（M1–M8 全绿）：
  - 修复 Tokenizer 空白字符循环控制：空白 token 产生后立即 `continue`，避免缩进空白被误记为 `UNKNOWN`。
  - 补充 runtime 读取路径兼容：`tests/mX/...` 自动映射到 `tests/milestones/mX/...`（文件存在时）。
  - 增加 runtime 错误旗标并在 `main` 中映射为稳定非零退出码（修复 `m5_t06_fail_fast`）。
  - 修复 mixed-match 字符串提升的生命周期错误（不再返回栈内临时缓冲区指针）。
- Runtime C ABI 首轮收敛：
  - `styio_file_open*` 从 `exit(1)` 改为返回 `0` 句柄。
  - 句柄从裸指针整数改为受控表映射。
  - 新增 `styio_free_cstr` 并在写文件追加换行路径回收拼接内存。

## Checkpoint

### 状态

当前主干已具备“可中断、可恢复”的最小基础：构建不依赖仓库内二进制，诊断可结构化输出，会话级生命周期接管已开始。

### 下一步

1. C.3：AST RAII 第一段（先 Expr 节点族）。
2. D.1：建立单线程 soak 测试框架。
3. C.8 深化：句柄表接管路径补齐更多 runtime 分支。

### 复现命令

```bash
cmake -S . -B build
cmake --build build --target styio styio_test styio_security_test
ctest --test-dir build -L security --output-on-failure
ctest --test-dir build -L styio_pipeline --output-on-failure
./build/bin/styio --error-format=jsonl --file sample/hello_world.styio
```

### 风险与回滚点

- 句柄表迁移目前只覆盖运行时文件 API，尚未补充完整并发/长跑压力覆盖。
- 字符串拼接释放路径当前仅在“写文件追加换行”路径接入，二元字符串加法全链路回收仍待后续 checkpoint。

## Checkpoint（Unicode Facade）

### 当前状态

前端 Unicode 分类路径已经集中到 `StyioUnicode`，接口对上层只暴露标准 C++ 类型；ICU 仍保持可选依赖且实现细节不外泄。

### 下一步

1. 评估并定义“是否开放 Unicode 标识符”的语言语义开关（保持默认兼容）。
2. 若确认替换 ICU，直接在 `StyioUnicode` 实现层切换到 `utf8proc/simdutf` 并补等价回归测试。
3. 推进 C.3（AST Expr 族 RAII）并引入对应 ASan 回归。

### 复现命令

```bash
cmake -S . -B build -DSTYIO_USE_ICU=OFF
cmake --build build --target styio styio_security_test styio_test
ctest --test-dir build -L security --output-on-failure
ctest --test-dir build -L styio_pipeline --output-on-failure
ctest --test-dir build -L milestone --output-on-failure
```
